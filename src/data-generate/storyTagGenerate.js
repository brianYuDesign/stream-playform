import helpers from "../helpers"

export default async tmpAdminUser => {
  await helpers.storyTag.create({
    input: { name: "騷貨" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "內射" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "AV" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "白虎" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "大奶" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "sexy" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "騷" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "性感" },
    user: tmpAdminUser
  })
  await helpers.storyTag.create({
    input: { name: "實戰" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "生活" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "日常" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "露臉" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "口爆" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "A片" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "真的主播" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "顏控" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "舔馬眼" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "幹炮" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "實戰" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "偷情" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "奶" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "屌" },
    user: tmpAdminUser
  })

  await helpers.storyTag.create({
    input: { name: "大雞巴" },
    user: tmpAdminUser
  })
}
