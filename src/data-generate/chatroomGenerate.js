import helpers from "../helpers"
import { getRandomObject } from "../utils"

export default async () => {
  const artists = await helpers.user.getByFilter({
    roleFeature: await helpers.roleFeature.getRoleArtistId()
  })

  const clients = await helpers.user.getByFilter({
    roleFeature: await helpers.roleFeature.getRoleClientId()
  })

  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
  await helpers.chatroom.artistChatroom({
    artist: getRandomObject(artists).id,
    user: getRandomObject(clients)
  })
}
